// Because this is a arrow function, if you just have one parameter
// You can set it without parenthesis
// If you have more than 2, you'll have to use them
const from = arr => {
    return Array.prototype.slice.call( arr );
}

/* FUNCTIONS ARE ALSO OBJEEEEEEEECTS */ 