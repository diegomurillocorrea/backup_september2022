/*
merge( L,R )
[ 3,27 ], [ 9,10 ]
1. Initialize an empty array
2. Compare the first index of the left array
to the first index of the right array
3. Push the lower value to the empty array
4. Shift the array with the lower value
5. Repeat until both array are empty
*/





/*const merge = ( L,R ) => {
    var array = []
    if ( L[0] == R[0] ) {
        array[] = 
    }
}

console.log( [ 3,27 ], [ 9,10 ] )*/